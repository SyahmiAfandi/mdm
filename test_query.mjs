import dotenv from 'dotenv'; dotenv.config(); import { createClient } from '@supabase/supabase-js'; const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function check() { const {data, error} = await supabase.from('report_extraction_tracker').select('count', { count: 'exact', head: true }); console.log('Tracker Count:', data, error); const {data: data2, error: error2} = await supabase.from('recon_cells').select('count', { count: 'exact', head: true }).eq('status', 'mismatch'); console.log('Mismatch Count:', data2, error2); } check();
