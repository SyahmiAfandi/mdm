require('dotenv').config(); const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function check() { const {data: rpc, error: rpcErr} = await supabase.rpc('sync_mismatches_to_task_list', { p_task_list_id: '8f9bd61d-3a3f-4da5-9618-d92708989621', p_period_id: '2026-03', p_report_type: 'All', p_user: 'Admin' }); console.log('RPC Result:', rpc, rpcErr); } check();
