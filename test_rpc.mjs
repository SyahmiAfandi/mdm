import dotenv from 'dotenv'; dotenv.config(); import { createClient } from '@supabase/supabase-js'; const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function check() { const {data, error} = await supabase.from('recon_cells').select('id, period_id, business_type, report_type_name').eq('status', 'mismatch').limit(3); console.log('Cells:', data); const {data: rpc, error: rpcErr} = await supabase.rpc('sync_mismatches_to_task_list', { p_task_list_id: '736d8c84-0e92-4db0-ab52-cf606270575a', p_period_id: data[0].period_id, p_report_type: 'All', p_user: 'Admin' }); console.log('RPC Result:', rpc, rpcErr); } check();
